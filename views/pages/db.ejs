<!DOCTYPE html>
<html>
    <head>

    </head>

    <body>
        
        <div class="container">
            <h2>Database Results</h2>
            <ul>
                <% rows.forEach(function(r) { %>
                    <li><%= r.userid %>, <%= r.age %> </li>
                <% }); %>
            </ul>
        </div>
    </body>
</html>